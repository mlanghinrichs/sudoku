Add "if this is the only possible x in row, col, sqr..." logic to solver
Add "if this is the only row with possible x's in a given square, remove those from possibilities elsewhere"
Burn this whole project to the ground
